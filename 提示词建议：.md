提示词建议：
你可以将此Markdown文档复制后，配合以下提示词发给AI工具：

“请根据这份需求设计文档，为我开发一个Flutter移动应用。要求如下：

1.  技术栈：Flutter (Dart)，使用 hive 作为本地数据库，provider 或 riverpod 作为状态管理。

2.  核心功能：必须完整实现 3.2. 肌肉记忆练习模式 的所有逻辑，特别是 FR-2.3 核心验证逻辑。

3.  数据模型：严格按照 第5章 数据模型 定义 Word, Dictionary, StudyLog 三个Hive Object。

4.  文件导入：实现 FR-1.3 本地文件导入 和 IF-1 词库文件格式规范，确保能解析 | 分隔的文本文件。

5.  离线优先：整个应用架构必须保证核心功能在无网络环境下可用。

6.  代码质量：请提供清晰的文件结构和注释，并生成 pubspec.yaml 依赖列表。”